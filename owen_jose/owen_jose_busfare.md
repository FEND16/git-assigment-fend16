# Psuedokod

__start__ </br></br>

```javascript
var k
var amount = prompt() med en loop och if-sats for att begränsa inmatnigsvärde mella 100-10000
funktion busCard(k,amount)
    var counter = 0
    var compare = amount - k
    while loop compare > 0
        if compare >= 500
            compare -= 500
            counter++
        else if 100 <= compare < 500
            compare -= 500
            counter++
        else
            compare -= 500
            counter++

    console.log(counter)
```