# Pseudokod

### Start
```
function busfare(k)    
    var addMoney = k
    var transactions = 0
    
    while addMoney > 0
        if addMoney >= 500
            addMoney - 500
            transactions + 1
        
        else if addMoney >= 101
            addMoney - 200
            transactions + 1
        
        else
            addMoney - 100
            transactions + 1
     
     return Åka för? 800. Antal transaktioner: 3
     
busfare(800)
```
        
### Stop
