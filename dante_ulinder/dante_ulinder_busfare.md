# Pseudokod

### Start
```javascript
function busfare(k)    
    var addMoney = k
    var transactions = 0
    
    while addMoney > 0
        if addMoney >= 500
            addMoney - 500
            transactions + 1
        
        else if addMoney >= 200
            addMoney - 200
            transactions + 1
        
        else
            add money - 100
            transactions + 1
     
     return Åka för? k Antal transaktioner: transactions
     
busfare(800)
```
        
### Stop
